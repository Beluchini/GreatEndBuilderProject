@page "/"
@using BuildShop.Database
@using BuildShop.Model
@rendermode InteractiveServer
@inject BuildContext ef
<PageTitle>Home</PageTitle>
<div class="CatalogHead">
    <div class="DP1">
        <a href="https://localhost:44304/" class="Name">СтройДом</a>
        <a href="https://localhost:44304/" class="Catalog">Каталог</a>
        <input class="Quest" type="text"/>
    </div>
</div>
<div class="AllProducts">
    <div class="parent">
        <div class="parent">
            @foreach (var item in ef.Products.ToList())
            {
                <div class="Products">
                    <img class="Imag" src="@item.Style">
                    <p class="Price"> @item.Price рублей</p>
                    <p class="Cound">за @item.Count упаковку</p>
                    <p class="Text">@item.Title</p>
                    <p class="Categ">@item.Kind</p>
                    <a class="Buy" href="https://localhost:44304/delivery" type="submit">Купить</a>
                </div>
            }
        </div>
    </div>
</div>

@code
{
    private Product _product = new Product();
    
  /*  private async Task Shop()
    {
        ef.Update(_product);
        await ef.SaveChangesAsync();
        StateHasChanged();
    } */
} 